
<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <p>
    <%= link_to(user.username, user) %>
    <!--Tässä oli tarkistus onko käyttäjä kirjautunut ja onko käyttäjä admin. Aiheutti hirveän kasan SQL-kyselyitä mokoma --!>
    <span class="badge text-bg-danger"><%= user.enabled %></span>
    </p>
    <p>Has made <%= "#{user.ratings.size}"%> ratings, average rating <%= "#{user.average_rating}" %></p>
  <% end %>
</div>

